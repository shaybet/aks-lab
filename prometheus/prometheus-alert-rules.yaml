groups:
  - name: nginx-alerts
    rules:
      - alert: HighCPUUsage
        expr: sum(rate(container_cpu_usage_seconds_total{container="nginx"}[5m])) by (pod) > 0.5
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High CPU usage detected in Nginx pod"